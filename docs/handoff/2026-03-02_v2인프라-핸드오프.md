# 핸드오프 — v2 플랫폼 인프라 기반 작업 (2026-03-02 저녁)

**날짜**: 2026-03-02 | **브랜치**: claude/v53-tab-naver-model

---

## 이번 세션에서 한 것

### v2 인프라 기반 완료

| 파일 | 내용 |
|------|------|
| `deploy.yml` (0-v2단계 추가) | Node.js 20 LTS + pm2 설치, v2 nginx 블록 생성, /home/ubuntu/CORTHEX_V2 디렉토리 준비 |
| `.github/workflows/deploy-v2.yml` (신규) | v2 브랜치 → 서버 자동 배포 workflow |

### v2 서버 구조 (다음 배포 시 자동 세팅됨)

```
corthex-hq.com     → nginx → Python uvicorn (포트 8000) ← 기존 그대로
v2.corthex-hq.com  → nginx → Node.js/Next.js (포트 3000) ← 신규
```

---

## 다음 세션 시작 순서

### 1. Cloudflare DNS 추가 (대표님이 직접)
- Cloudflare 대시보드 → corthex-hq.com → DNS
- **A 레코드 추가**: 이름 `v2`, 값 = 서버 IP (기존 corthex-hq.com과 같은 IP), 프록시 ON

### 2. 스케치바이브 아키텍처 이미지 공유
- 대표님이 NEXUS 스케치바이브에서 그린 구조도 이미지로 공유
- 기술스택 확정 (Next.js 추천) + 상세 계획 수립

### 3. v2 앱 개발 시작
- 새 `v2` 브랜치 생성 후 Next.js 앱 초기화
- deploy-v2.yml 자동 배포 연동

---

## 확정된 결정사항

| 항목 | 결정 |
|------|------|
| 언어 | **JavaScript** (Python 아님) |
| 방식 | 기존 서버 유지 + 같은 서버 포트 분리 |
| 도메인 | v2.corthex-hq.com |
| v2 포트 | 3000 (Node.js/Next.js) |
| 프로세스 관리 | pm2 |

---

## 다음 세션 시작 프롬프트

```
안녕. docs/handoff/2026-03-02_v2인프라-핸드오프.md 읽어.
스케치바이브 구조도 그렸어, 이미지 공유할게.
[이미지]
```
