# 2026-03-01 — 누나 CLI 자격증명 연동

## 완료 내용
- `/home/ubuntu/.claude-sister/.claude/.credentials.json` 생성
- 누나 Claude Max OAuth 토큰 등록 (accessToken + refreshToken)
- refreshToken으로 자동 갱신됨 → 수동 관리 불필요

## 검증 결과
| 에이전트 | 응답 | 비용 | cli_owner |
|----------|------|------|-----------|
| saju_eden | ✅ 정상 | $0.00 | sister |
| saju_zoe | ✅ 정상 | $0.00 | sister |
| saju_sage | ✅ 정상 | $0.00 | sister |

## 구조
- CEO 에이전트(7명) → `HOME=/home/ubuntu/` → `~/.claude/.credentials.json` → 대표님 Max 한도
- 누나 에이전트(3명) → `HOME=/home/ubuntu/.claude-sister/` → `.claude/.credentials.json` → 누나 Max 한도
- `config/agents.yaml`의 `cli_owner: ceo | sister`로 분기
- `web/ai_handler.py` `_call_claude_cli()` 695행에서 `cli_owner == "sister"` → HOME 경로 교체

## 파일 변경
- 서버 파일 직접 생성 (git 커밋 대상 아님, 서버 로컬 자격증명)
- `docs/todo/BACKLOG.md` — CLAUDE_API_KEY_SISTER 항목 ✅ 완료 처리
