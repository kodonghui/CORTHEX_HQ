# 2026-03-01 — UX 대개편 Phase 2~3 + v5 전략 구현

## 배포 현황
| 빌드 | 내용 |
|------|------|
| #733 | Phase 1: 네온/사이버 → 프리미엄 SaaS 팔레트 (violet #8b5cf6, 배경 #121216) |
| #734 | confirmModal Alpine.js 글리치 완전 제거 (x-if 교체) + 다크모드 밝기 상향 |
| #735 | Phase 2: 탭바 pill 스타일 + 사이드바 팀카드 아바타/상태배지 개선 |
| #736 | 다크모드 밝기 추가 상향 (bg #121216 → #1e1e26) |
| PR #712 | v5 구현 — 역할 기반 로그인 + 3본부 사이드바 + 기밀문서 탭 + CLI 라우팅 (배포 대기) |

---

## v5 구현 내용 (PR #712, 대표님 확인 후 배포)

### v5-1: 역할 기반 로그인
- 로그인 화면에 탭 추가: **대표님(violet)** / **누나(green)**
- 역할별 비밀번호 분리: `admin_password` (CEO) / `sister_password` (누나)
- 초기값: 대표님 `corthex2026`, 누나 `sister2026`
- `auth_handler.py`: role 파라미터 + 역할별 비밀번호 검증
- `corthex-app.js`: `auth.loginRole` 추가, `doLogin()` role 전송

### v5-2: agents.yaml org/cli_owner 필드
- 모든 에이전트에 `org` 필드 추가:
  - `chief_of_staff` → `org: common`
  - 팀장 5명 → `org: leet_master`
- `cli_owner: ceo` 필드 추가 (사주 에이전트 추가 시 `cli_owner: sister` 설정)
- `config_loader.py`에서 org/cli_owner 전달

### v5-3: 사이드바 3본부 구조
- 비서실장(공통) → 리트마스터 본부 → 스케치바이브(준비중) → 사주(준비중)
- sister 로그인 시: 리트마스터/스케치바이브 숨김, 사주 본부 green 강조
- CEO 로그인 시: 전체 표시 (기존과 동일)

### v5-4: 기밀문서 본부별 탭
- 7개 division 버튼 → 5개 본부 탭: 전체 / 리트마스터 / 스케치바이브 / 사주 / 공통
- `matchesBunbu(division, bunbu)` 헬퍼 함수 추가
- sister 로그인 시 자동으로 사주 탭 선택

### v5-5: 누나 CLI 라우팅
- `_call_claude_cli()`에 `cli_owner` 파라미터 추가
- `sister` → `CLAUDE_API_KEY_SISTER` 환경변수 사용 (미설정 시 Max 구독 폴백)
- `agent_router.py`: `_AGENTS_DETAIL`에서 `cli_owner` 읽어 `ask_ai()`에 전달

---

## 전략 확정 (Party Mode 결과, 이전 세션)

### 예창패
- **리트마스터로 확정**
- "API만 태운 거 아닌가" 우려 → 예창패는 직접 AI 개발 안 해도 됨

### 스케치바이브
- CORTHEX CLO에게 임시 출원 위임

### CORTHEX v5 조직 개편
```
3본부 체제:
├─ 리트마스터 본부 (대표님 프로젝트1)
├─ 스케치바이브 본부 (대표님 프로젝트2)
└─ 사주 본부 (누나 + 대표님 공동)
```

---

## 다음 세션 이어가기
1. PR #712 머지 + 배포 확인 (대표님)
2. 다크모드 밝기 확인 (빌드 #736)
3. 누나 비밀번호 변경 (`sister2026` → 원하는 값)
4. Phase 3 구현 여부 결정 (채팅버블/대시보드 카드)
5. `CLAUDE_API_KEY_SISTER` GitHub Secrets 등록 (누나 계정 생기면)
