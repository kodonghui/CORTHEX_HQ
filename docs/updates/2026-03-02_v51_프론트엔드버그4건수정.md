# v5.1 프론트엔드 버그 4건 수정 (빌드 #765)

**날짜**: 2026-03-02 (KST 03:41)
**브랜치**: claude/v51-bugfix-4 → main 머지

---

## 수정 내용

### 버그 1: CEO 사이드바 에이전트 안 보임 🔴 → ✅
- **원인**: x-show="workspace.sidebarFilter === 'ceo'" 하드코딩 + /api/agents에 cli_owner 미포함
- **수정**:
  - `AgentConfig`에 cli_owner/org/dormant/telegram_code 필드 추가
  - `/api/agents` 응답에 cli_owner, dormant 포함
  - JS에 `agentCliOwner` 매핑 객체 추가
  - 사이드바 x-show → `agentCliOwner[id] === workspace.sidebarFilter` 기반 동적 필터링

### 버그 2: 사무실 뷰 구형 하드코딩 🟡 → ✅
- **원인**: "CORTHEX STAFF — 팀장 6명" 하드코딩 (PR #730이 사무실은 안 건드림)
- **수정**: `workspace.officeLayout` 기반 `x-for` 동적 렌더링
  - CEO: 4섹션 (COMMAND CENTER + 리트마스터 + 사주 + 금융)
  - 누나: 1섹션 (사주냥 TEAM)
  - CEO 카드는 sidebarFilter='ceo'일 때만 표시

### 버그 3: 로그아웃 버튼 안 보임 🔴 → ✅
- **원인**: `auth.bootstrapMode`가 true 유지 → 버튼 숨김
- **수정**: x-if 조건에서 bootstrapMode 제거 → `auth.token`만 확인

### 버그 4: 전력분석 시체 에이전트 🟡 → ✅
- **원인**: performance DB + soul_gym_rounds에 구 에이전트 ID 기록 잔존
- **수정**:
  - `loadPerformance()`: agentNames에 없는 agent_id 필터 아웃
  - `loadGymHistory()`: agentNames에 없는 agent_id 필터 아웃
  - survey_specialist, infra_specialist, cio_manager 등 전부 UI에서 제거

## 수정 파일
| 파일 | 변경 |
|------|------|
| src/core/agent.py | AgentConfig에 cli_owner/org/dormant/telegram_code 추가 |
| web/app.py | /api/agents에 cli_owner, dormant 포함 |
| web/static/js/corthex-app.js | agentCliOwner 매핑 + loadPerformance 필터링 |
| web/templates/index.html | 사이드바+사무실+로그아웃+Soul Gym 수정 |

## 검증
- 빌드 #765 배포 성공
- /api/agents → cli_owner 정상 포함 (ceo 7명, sister 3명)
- 서버 로그 에러 없음
