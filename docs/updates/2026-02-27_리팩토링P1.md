# 2026-02-27 arm_server.py 리팩토링 P1 — 빌드 #656

## 요약
arm_server.py에서 설정/유틸리티 코드를 config_loader.py로 분리 (Phase 1/8)

## 변경 내역

| # | 파일 | 변경 |
|---|------|------|
| 1 | `web/config_loader.py` | **신규** — 유틸+설정+에이전트 로딩 (343줄) |
| 2 | `web/arm_server.py` | 추출된 코드 제거 + config_loader import 추가 |

## 추출된 함수/상수

| 카테고리 | 항목 |
|----------|------|
| 유틸리티 | `_log()`, `_extract_title_summary()`, `_diag` |
| 환경로딩 | `_load_env_file()` (auto-execute) |
| 설정로드 | `_load_config()`, `_load_agents()`, `_load_tools()` |
| 데이터영속 | `_load_data()`, `_save_data()`, `_save_config_file()` |
| 에이전트 | `_sync_agent_defaults_to_db()`, `_build_agents_from_yaml()`, AGENTS |
| 상수 | KST, BASE_DIR, CONFIG_DIR, DATA_DIR, MODEL_*_MAP 등 |

## 줄 수 변화
- arm_server.py: 11,637 → 11,343줄 (**294줄 감소**)
- config_loader.py: 343줄 (신규)

## 다음 단계
- P2: 도구+Soul Gym+디버그 추출 (`web/soul_gym.py`, `web/debug_api.py`, `web/tool_mgr.py`)
