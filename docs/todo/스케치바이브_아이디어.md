# 스케치바이브 (SketchVibe) — 아이디어 기록

> 최초 논의: 2026-02-28
> 대표님 아이디어. 특허 출원 검토 중.

---

## 한 줄 요약

> **손으로 대충 그린 스케치 + 말(자연어)을 동시에 입력하면, AI가 의도를 파악해서 정확한 다이어그램으로 변환하고, 확인 후 실제 구현까지 이어지는 시스템**

---

## 왜 이게 필요한가 (탄생 배경)

자율 AI 에이전트 시스템을 만드는 사람에게 가장 큰 병목은:

> **"머릿속 구조를 AI한테 정확하게 전달하는 것"**

말로만 설명하면 오해가 생기고, 코드로 직접 쓰면 비개발자는 못 한다.
스케치로 그리면서 말하는 게 가장 빠르고 자연스럽다.

실제로 2026-02-28 논의에서 말로 설명하다가 다이어그램 보여주자마자
"어 맞아 그거야!"가 나온 것이 이 아이디어의 직접적 계기.

---

## 핵심 파이프라인

```
① 대표님이 손으로 스케치 (이미지)
        ↓
② AI가 이미지 읽음 → Mermaid 코드로 변환
   (이미지보다 코드가 구조를 더 정확하게 표현)
        ↓
③ Claude가 Mermaid 코드 + 자연어 설명 동시에 읽음
   → 의도 / 방향성 / 맥락 파악
        ↓
④ "아, 이 말씀이시군요~" → 정확한 다이어그램으로 보정
   원본 스케치 위에 덧그리거나 수정해서 보여줌
        ↓
⑤ 대표님 "맞아" 확인 (교정 포인트)
        ↓
⑥ Claude 실제 구현
```

---

## 핵심 기술 통찰 3가지

### 1. Mermaid를 중간 언어로 쓰는 것
이미지를 바로 읽는 게 아니라 **Mermaid 코드로 변환한 뒤 읽는다.**

왜 이게 중요한가:
- 이미지 직접 읽기: 정확도 80% (박스 겹침, 글씨 흐릿하면 실수)
- Mermaid 코드: 정확도 100% (구조가 명확하게 텍스트로 표현됨)

### 2. 자연어 + 스케치 동시 입력 (멀티모달)
자연어만: AI가 형태를 모름
스케치만: AI가 의도/맥락을 모를 수 있음
**둘 동시에**: 시각 정보 + 의미 정보가 합쳐져 의도 추론 정확도가 비약적으로 올라감

### 3. 중간 확인 단계 (⑤번)
이미지 → AI → 바로 구현: 잘못 이해해도 대표님이 모름
이미지 → Mermaid → "이렇게 이해했습니다" → **대표님 확인** → 구현

> 비행기 자동항법이 아니라, 기장이 경로를 확인하는 체크포인트가 있는 구조.
> 이게 단순 편의기능이 아니라 신뢰성의 핵심.

---

## 다이어그램 종류 (이 시스템이 처리하는 것)

2026-02-28 논의에서 정의한 두 가지:

| 종류 | 설명 | 예시 |
|------|------|------|
| **연결도** | 파일/모듈/시스템이 서로 어떻게 연결됐는지 관계도 | arm_server.py → trading_engine.py → kis_client.py |
| **흐름도** | 버튼 클릭 → 어느 파일 → 결과까지 순서도 | 사용자 클릭 → handler → engine → DB → 화면 |

스케치바이브는 이 두 종류를 손으로 대충 그려도 정확한 형태로 변환해준다.

---

## 기존 서비스와 차이점

| | getsketchvibe.com (기존) | 스케치바이브 (대표님 개념) |
|---|---|---|
| **입력** | 텍스트만 | 스케치 + 자연어 동시 |
| **출력** | 연필화/수채화 이미지 | 정확한 시스템 다이어그램 |
| **목적** | 예술적 이미지 생성 | 시스템 구조 이해 + 구현 |
| **특화** | 일반 미술 | AI 에이전트 조직도/시스템 구조 |

**완전히 다른 서비스다.** 이름만 비슷할 뿐.

---

## 이름 문제

- **특허**: 기술(방법/프로세스)에 내는 것 → getsketchvibe.com 존재와 무관하게 출원 가능
- **상표**: "스케치바이브" 이름 자체 → 기존 서비스와 충돌 가능성 있음. 출원 전 검토 필요

> 특허는 가능, 이름(상표)은 재검토 필요.

---

## 기존 특허 충돌 위험 (2026-02-25 법무 검토 결과)

| 특허 | 보유자 | 충돌 수준 |
|------|--------|----------|
| US10,817,072 | Microsoft | ⚠️ 중간 (손그림→HTML 구조 일부 중첩) |
| US11,151,310 | Adobe | 🔴 높음 (스케치+AI+코드 출력 3요소 중첩) |

**차별화 포인트 (충돌 회피 핵심)**:
- 자연어 + 스케치 **동시** 멀티모달 입력 → 이건 선행기술 없음
- AI 에이전트 조직도 특화 어휘/구조 인식
- 비개발자 대상 (일반 스케치 변환 아님)

---

## 현재 구현 상태

- Nexus 기능에 프로토타입 존재 (`docs/architecture/nexus-design.md`)
- Mode 2: 비주얼 캔버스 (Drawflow 라이브러리) — 노드 팔레트, 드래그 연결, JSON 저장
- 스케치 이미지 → Mermaid 변환 파이프라인: **미구현**
- 자연어 + 스케치 동시 입력 인터페이스: **미구현**

---

## 다음 단계 (논의 필요)

- [ ] 이름 재검토 (상표 충돌 가능성)
- [ ] 특허 출원 범위 확정 (법무팀장과 논의)
- [ ] Nexus 프로토타입을 스케치바이브 파이프라인으로 확장
- [ ] 스케치 이미지 → Mermaid 변환 구현 방식 결정
- [ ] MVP 범위 정의 (연결도만? 흐름도도?)

---

> 이 문서는 2026-02-28 대표님과의 논의를 바탕으로 작성.
> 특허 관련 추가 논의 시 법무팀장 참여 필요.
