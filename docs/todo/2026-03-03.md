# 2026-03-03 완료 기록

## ✅ NEXUS Cytoscape.js 전환 (ab08e04~576ea8c)

Mermaid.js 네이티브 → Cytoscape.js 엔진으로 전면 교체
- Cytoscape.js + dagre + lodash CDN 동적 로드
- 노드 8종 시각 스타일 (agent/system/api/decide/db/start/end/note)
- 드래그 이동, 더블클릭 편집, Delete 삭제
- cytoscapeToMermaid() / mermaidToCytoscape() 양방향 변환
- 레거시 Mermaid/Drawflow 자동 마이그레이션
- index.html 함수명 7곳 교체 + 상태 바 + 캔버스 div 변경

## ✅ NEXUS 무한 로딩 수정 (b74d91f)

1. `_loadScript` CDN 캐시 오염 — 실패 Promise 영구 캐시 → delete + dataset.loaded
2. `openNexus` DOM 타이밍 — setTimeout(200) → $nextTick + 폴링
3. `initCytoscapeCanvas` 컨테이너 null — 에러 화면 + 재시도 버튼

## ✅ Cloudflare/nginx 캐시 문제 해결 (e2b1eee~fa0210c)

근본 원인: nginx가 `/var/www/html/`에서 정적 파일 서빙, deploy-fast.sh가 이 디렉토리 미동기화
- arm_server.py: Cache-Control: no-cache 헤더 추가
- index.html: BUILD_NUMBER_PLACEHOLDER 원복 (하드코딩 실수 수정)
- sw.js: CACHE_NAME v1→v2

## ✅ NEXUS Shift 연결 모드 + 멀티노드 연결 (de6875b)

- Shift 키로 연결 모드 ON/OFF 토글
- Ctrl+클릭 다중 선택 → Shift → 대상 클릭 = 일괄 연결
- 연결 완료 후 자동 모드 해제

## ✅ deploy-fast.sh 개선 (b0bbf81 + 미커밋)

1. nginx 정적 파일 자동 동기화 (JS/CSS/SW/HTML)
2. HTML BUILD_NUMBER_PLACEHOLDER → 타임스탬프 치환
3. Actions 취소: sleep 5 → 3초×10회 폴링 (최대 30초)

## ✅ 사이드바 에이전트 전원 안 뜨는 버그 수정 (1e0f435)

원인: Alpine.js 반응성 — `agentCliOwner = {}` 빈 객체에 `obj[key] = value` 동적 키 추가 시 변경 감지 안 됨
- loadAgentsAndTools()에서 객체 뮤테이션 → 새 객체 교체 방식으로 변경 (8개 맵 전부)
- init()에서 loadAgentsAndTools()에 await 추가 (사이드바 렌더 전 데이터 보장)

## ✅ agents.json 구 ID → 신 ID 재생성 (f5b1d05)

근본 원인: agents.yaml은 리네이밍(leet_strategist 등) 완료됐는데, agents.json은 구 ID(cso_manager) 그대로.
config_loader가 JSON 우선 로드 → HTML 하드코딩 ID와 불일치 → 사이드바 매칭 실패
- yaml2json.py 로직으로 agents.json + tools.json + workspaces.json 재생성

## ✅ CLAUDE.md 구조 개선

규칙 미준수 원인 분석 후 구조 개편:
- @import를 파일 맨 아래로 이동 (절대 규칙이 컨텍스트 앞에 오도록)
- 🔴 마커 인플레이션 제거 (6곳 → 0곳, 🚨 3개만)
- 상태형 규칙("항상 지킬 것")과 절차형 규칙(STEP 1-6)을 물리적 분리
